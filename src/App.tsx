import { Button } from '@mui/material';
import { CssBaseline } from '@mui/material';

const App = () => {
   return (
      <div>
         <CssBaseline />
         <Button>
            This is button
         </Button>
      </div>
   )
}

export default App;
